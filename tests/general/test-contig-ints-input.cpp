#include "rmacxx.hpp"
#include <cassert>
#include <array>
#include <deque>
#include <list>

int main(){
    // constexpr bool init_list_passes = rmacxx::input_contig_ints<std::initializer_list<int>>;
    // constexpr bool vec_passes = rmacxx::input_contig_ints<std::vector<int>>;
    // constexpr bool array_passes = rmacxx::input_contig_ints<std::array<int,8>>;
    // constexpr bool c_array_passes = rmacxx::input_contig_ints<int*>;
    // constexpr bool dequeue_passes = rmacxx::input_contig_ints<std::deque<int>>;
    // constexpr bool doubly_linked_list_passes = rmacxx::input_contig_ints<std::list<int>>;
    // assert(init_list_passes);
    // assert(vec_passes);
    // assert(array_passes);
    // assert(!c_array_passes);
    // assert(!dequeue_passes);
    // assert(!doubly_linked_list_passes);

    // constexpr bool f_init_list_passes = rmacxx::input_contig_ints<std::initializer_list<float>>;
    // constexpr bool f_vec_passes = rmacxx::input_contig_ints<std::vector<float>>;
    // constexpr bool f_array_passes = rmacxx::input_contig_ints<std::array<float,8>>;
    // constexpr bool f_c_array_passes = rmacxx::input_contig_ints<float*>;
    // constexpr bool f_dequeue_passes = rmacxx::input_contig_ints<std::deque<float>>;
    // constexpr bool f_doubly_linked_list_passes = rmacxx::input_contig_ints<std::list<float>>;
    // assert(!f_init_list_passes);
    // assert(!f_vec_passes);
    // assert(!f_array_passes);
    // assert(!f_c_array_passes);
    // assert(!f_dequeue_passes);
    // assert(!f_doubly_linked_list_passes);
}
