#include "rmacxx.hpp"
#include <list>

#include <cassert>

int main(int argc, char *argv[])
{

	// MPI_Init(&argc, &argv);

	// int num1, num2, num3, num4, num5;

    // rmacxx::Window<int, LOCAL_VIEW> win({10,10});
    // win.fill(1);

    // if (win.rank() == 0) {
    //     const bool list_ok = rmacxx::is_contig<std::list<int>>;
    //     assert(!list_ok);
    //     const bool vector_ok = rmacxx::is_contig<std::vector<int>>;
    //     assert(vector_ok);
    //     const bool array_ok = rmacxx::is_contig<std::array<int,5>>;
    //     assert(array_ok);
    // }

    // win.wfree();

	// MPI_Finalize();

	// return 0;
}